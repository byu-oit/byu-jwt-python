version: 0.1

phases:
  install:
    commands:
      - echo "Starting Command Phase"
  pre_build:
    commands:
      - echo "pre-build"
      - npm install https
      - npm install aws-sdk
      - npm install https
      - npm install request
  build:
    commands:
      - echo "Build started on `date`"
  post_build:
    commands:
      - echo "Build completed on `date`"

artifacts:
  files:
    - ./**/*
